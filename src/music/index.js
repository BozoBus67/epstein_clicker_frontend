// The music folder's public surface is the Audio_Controls component — the
// three-button row (music player, volume, shuffle). Internal sub-components
// (Music_Player, Volume_Control, Shuffle_Button) are not re-exported.
export { default } from './audio_controls';
